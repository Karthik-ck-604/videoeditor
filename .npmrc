auto-install-peers=false
strict-peer-dependencies=false
node-linker=hoisted
# NOTE: This project lives on an exFAT drive which does not support symlinks.
# install.sh creates a symlink from node_modules to /tmp/editor_hiring_node_modules
# (ext4) so that pnpm can freely use symlinks within the node_modules directory.